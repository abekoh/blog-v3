---
import Layout from "../../layouts/Layout.astro";
import { getCollection } from "astro:content";
const allPosts = await getCollection("posts");
---

<Layout title="Welcome to Astro.">
  <h1>Posts</h1>
  <ul>
    {allPosts.map((post) => <li>{post.data.title}</li>)}
  </ul>
</Layout>
